{
  "name": "scripts",
  "version": "0.0.1",
  "type": "module",
  "description": "A collection of scripts",
  "exports": [
    "./index.js"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "node --env-file=.env --no-warnings=ExperimentalWarning --loader ts-node/esm/transpile-only",
    "dev:pull-wiki": "npm run dev src/wiki.ts",
    "dev:gen-final-data": "npm run dev src/generate-final-data.ts",
    "dev:upsert-data": "npm run dev src/qdrant.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "contributors": [
    {
      "name": "Son Nguyen",
      "email": "son.nguyen@codeenginestudio.com",
      "url": "https://github.com/ces-sonnguyen"
    }
  ],
  "dependencies": {
    "@qdrant/qdrant-js": "^1.6.0",
    "node-fetch": "^3.3.2",
    "openai": "^4.20.1",
    "rimraf": "^5.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">= 20.9.0"
  }
}
